<!DOCTYPE html>
<html>
    <head>
        <title>admin content page</title>        
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <link type="text/css" rel="stylesheet" href="/stylesheets/materialize.min.css"  media="screen,projection"/>

        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    </head>
    <body>
        
          <div id="content-modal" class="modal">
            <div class="modal-content">
              <h4>Changer le contenu</h4>
              <div class="row">
                <form class="col s12">
                  <div class="row">
                    <div class="input-field col s12">
                      <textarea id="content-area" class="materialize-textarea" rows="25"></textarea>
                      <label for="content-area" id="label-content">Contenu (html)</label>
                    </div>
                  </div>
                </form>
              </div>
            </div>
            <div class="modal-footer">
                <a class="waves-effect waves-green btn-flat save-content">
                    <input type="hidden" id="id-content" />
                    sauvegarder
                </a>
            </div>
          </div>

        <h4>Interface gestion de contenu page <%= page %></h4><br>
        <div class="row">
        <% contents.forEach(function(content){ %>
        <% var html = (content.content.length > 150 ? content.content.substring(0, 150) + " . . . " : content.content); %>
            <div class="col s12 m3">
              <div class="card blue-grey darken-1">
                <div class="card-content white-text">
                  <span class="card-title">Contenu n°<%= content.order %></span>
                  <p><%= html %></p>
                </div>
                <div class="card-action">
                    <a class="change-content">
                        <input type="hidden" class="content-id" value="<%= content.id %>" />
                        <input type="hidden" class="content-content" value="<%= content.content %>" />
                        Changer le contenu
                    </a>
                    <a class="delete-content">
                        <input type="hidden" value="<%= content.id %>" />
                        Supprimer le contenu
                    </a>
                </div>
              </div>
            </div>        
        <% }); %>
        </div>
        
        <div class="fixed-action-btn">
          <a class="btn-floating btn-large blue lighten-3 add-content">
            <input type="hidden" value="<%= idpage %>" />
            <i class="large material-icons">add</i>
          </a>
        </div>
        
        <script type="text/javascript" src="/javascripts/jquery.js"></script>
        <script type="text/javascript" src="/javascripts/materialize.min.js"></script>
        <script type="text/javascript" src="/javascripts/sweetalert.js"></script>
        <script type="text/javascript" src="/javascripts/imports.js"></script>
        <script type="text/javascript" src="/javascripts/admin-actions.js"></script>
    </body>
</html>